import 'package:flutter/material.dart';

class InstructionStepModal extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: AlertDialog(title: Text('hello')));
  }
}
