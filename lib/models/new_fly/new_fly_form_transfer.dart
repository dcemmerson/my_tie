import 'fly.dart';
import 'new_fly_form_template.dart';

class NewFlyFormTransfer {
  final Fly flyInProgress;
  final NewFlyFormTemplate newFlyFormTemplate;

  NewFlyFormTransfer({this.flyInProgress, this.newFlyFormTemplate});
}
